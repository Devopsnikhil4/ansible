- name: Copy the redis repo
  ansible.builtin.copy:
    src: redis.repo
    dest: /etc/yum.repos.d/redis.repo

- name: Installing redis
  ansible.builtin.package:
    name: redis-6.2.11
    state: present





   